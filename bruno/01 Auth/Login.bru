meta {
  name: Login
  type: http
  seq: 1
}

post {
  url: {{baseUrl}}/user/login
  body: json
  auth: none
}

body:json {
  {
    "username": "super_admin",
    "password": "Admin123!@#"
  }
}

vars:pre-request {
  baseUrl: http://localhost:3000
}

docs {
  Login manual (opcional). El resto de requests ya hacen auto-login al pulsar Send.
}
