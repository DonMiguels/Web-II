meta {
  name: TX 18 · getStockById
  type: http
  seq: 13
}

post {
  url: {{baseUrl}}/
  body: json
  auth: none
}

body:json {
  {
    "transaction_id": 18,
    "profile": "admin",
    "lang": "es",
    "data": {
      "id": 1
    }
  }
}

vars:pre-request {
  baseUrl: http://localhost:3000
}

docs {
  Inventory.Stock.getStockById
  transaction_id=18 | profile=admin
  Auto-login al pulsar Send (environment local).
}
